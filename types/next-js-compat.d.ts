declare module 'next/types.js' {
  export type {
    Metadata,
    MetadataRoute,
    ResolvedMetadata,
    ResolvingMetadata,
    Viewport,
    ResolvingViewport,
    ResolvedViewport,
  } from 'next'
}
